import { MottoReplacerPipe } from './motto-replacer.pipe';

describe('MottoReplacerPipe', () => {
  it('create an instance', () => {
    const pipe = new MottoReplacerPipe();
    expect(pipe).toBeTruthy();
  });
});
