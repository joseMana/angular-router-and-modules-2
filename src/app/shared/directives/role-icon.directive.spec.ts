import { RoleIconDirective } from './role-icon.directive';

describe('RoleIconDirective', () => {
  it('should create an instance', () => {
    const directive = new RoleIconDirective();
    expect(directive).toBeTruthy();
  });
});
